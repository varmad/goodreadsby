# Copy to .env for local development. Real secrets live in the platform's secret
# store (SCRUM-3), never in the repository.

# FastAPI -> Postgres
DATABASE_URL=postgresql+psycopg://goodreadsby:goodreadsby@localhost:5432/goodreadsby

# Next.js -> FastAPI (read-only, server-side)
API_BASE_URL=http://localhost:8000
